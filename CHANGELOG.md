# 📜 ProxyProbe 版本更新日志 (Changelog)

本文档记录 **ProxyProbe** 的所有主要版本演进、核心功能迭代与 Bug 修复历史。

## 🌟 [v2.5.8] - 2026-09-25

### 📊 监控大盘、24小时时序流量走势与全景感知运维
- **首屏监控大盘 (Dashboard Overview)**：
  - 新增全景监控大盘，置顶首屏导航展示，包含全网节点健康率、总吞吐流量、有效订阅用户数与平均往返延迟四大核心 KPI。
- **24小时时序流量走势图 (24h Traffic Area Chart)**：
  - 原生自适应 SVG 矢量面积折线走势图，支持小时级数据聚合与双色渐变，任意屏幕尺寸（手机/电脑）自动缩放且无横向滚动条。
- **节点流量排行 & 协议分布矩阵**：
  - 直观呈现各节点流量消耗 TOP 榜与百分比进度条，一眼识别网络负载核心节点与协议占比。
- **节点全景感知矩阵 (Live Health Matrix)**：
  - 实时展示全部节点真实健康状态与往返延迟，支持一键「并发全网复测」与后台 10 分钟自动巡检。
- **Telegram 双向交互式运维指令引擎**：
  - 支持手机端对 Telegram Bot 发送 `/status`、`/test`、`/backup`、`/nodes` 远程运维，内置双向白名单严格鉴权。

---

## 🌟 [v2.5.7] - 2026-09-25

### 🛡️ 协议兼容性全面修复与真实连通性握手测试引擎
- **真实协议握手测试引擎**：
  - 彻底淘汰单纯 TCP 端口假阳性测试，通过拉起沙盒代理发起端到端真实网络请求验证数据流通与精确往返延迟。
- **Shadowsocks 2022 PSK 保护**：
  - 修复客户端订阅生成中误替换 16 字节 PSK 导致的客户端解密崩溃问题。
- **Trojan TLS 挂载**：
  - 为服务端 Trojan 挂载 TLS 证书并适配主流客户端强制 TLS 规范与证书校验。
- **SOCKS5 多用户鉴权支持**：
  - 修复 SOCKS5 服务端入站账号同步缺失问题，活跃订阅用户可正常鉴权连接。

---

## 🌟 [v2.5.0] - 2026-09-25

### 🔀 客户端分流规则深度自定义与预设中心 (Client Routing Rules Center)
- **管理后台新增「分流规则」独立配置模块**：
  - 路径 `/admin/rules`，采用直观的可视化标签页设计，为管理员提供全面的客户端路由分流定制能力。
- **开箱即用的四大经典规则预设**：
  - **国内外智能分流 (默认推荐)**：局域网与国内直连，常见海外域名与被阻断流量走代理，精准高效兼顾速度与隐私。
  - **极简轻量模式**：仅直连私有 IP 与中国顶级域名（`.cn`），其余默认走代理，启动极快。
  - **全局代理模式**：除本地回环与局域网外，所有流量强制走代理节点。
  - **自定义专家模式**：完全放开控制权，管理员可随心定制规则列表。
- **双协议客户端规则深度集成**：
  - **Clash (YAML)**：多行规则列表动态解析与实时语法展示，提供快捷标签插入功能（`DOMAIN-SUFFIX`、`DOMAIN-KEYWORD`、`IP-CIDR`、`GEOIP,CN,DIRECT`、`MATCH,PROXY` 等）。
  - **Sing-box (JSON)**：完整的 `route.rules` 数组编辑器，内置 JSON 格式化排版与实时语法有效性校验，杜绝非法 JSON 导致客户端崩溃。
- **全配置实时客户端预览与重置恢复**：
  - 提供「客户端配置实时预览」弹窗，可一键切换查看生成的完整 Clash YAML 与 Sing-box 客户端配置，所见即所得。
  - 提供「恢复默认」快捷按钮，新手管理员即便配置失误也可一键安全还原至默认最佳实践规则。

### 🔄 全自动版本检测与一键无损在线热升级 (Auto Update & One-Click Upgrade)
- **新版本红点与状态栏智能提醒**：
  - 管理后台启动时自动静默检测最新版本，发现新版本时顶部工具栏与左侧导航栏实时亮起跳动红点与「🔥 发现新版本 vX.X.X (点击升级)」徽章。
- **面板内一键在线热升级 (免命令行)**：
  - 管理后台提供全新设计的升级交互弹窗，展示当前版本、最新版本、发版日期及详细更新日志。
  - 点击「立即无损热升级」即可自动向服务器提交平滑热升级指令，无需登录 SSH 终端，自动同步最新核心二进制与前端静态资源并无缝重启服务，保留所有配置与数据库数据。
- **终端与面板双轨升级**：
  - 运维控制台 `hhub` 选项 `[9] 一键无损热升级系统` 同步升级，满足终端运维偏好。

### ⚡ 用户端视觉极简精炼 (Dashboard UI Polish)
- **精简核心订阅按钮文案**：
  - 用户中心仪表盘核心订阅按钮由「复制万能智能订阅」精简为「复制订阅」，减少视觉冗余，突出重点。
- **输入框与按钮间距美化**：
  - 调整订阅地址显示框与复制按钮内边距和布局，保持优雅统一。

---

## 🌟 [v2.4.0] - 2026-09-25

### ⚡ 万能自适应智能订阅 (Universal Smart UA Subscription)
- **单一订阅链接全客户端自动适配**：
  - 后端动态解析客户端 HTTP 请求的 `User-Agent`，实现零切换、无缝自适应下发最适合该客户端的订阅内容：
    - **Clash / Meta / Mihomo / Verge / Flclash / Stash**：自动下发包含规则分流组与代理策略集的标准化 Clash YAML 配置；
    - **Sing-box / SFA / SFI**：自动下发包含 Selector、URLTest 自动测速组与全套 Inbound/Outbound 的 Sing-box JSON 核心配置；
    - **Shadowrocket / v2rayN / Nekobox / 其他通用客户端**：自动下发标准化 Base64 聚合节点链。
- **灵活的手动覆盖参数**：
  - 支持通过 URL 参数强制指定输出格式（`?clash=1`、`?singbox=1`、`?b64=1`），满足用户自定义脚本或特殊客户端的需求。
- **用户自服务面板交互极致简约**：
  - 用户面板一键订阅卡片重构，重点突出醒目的「⚡ 复制万能智能订阅」核心按钮，搭配小火箭/Clash/Sing-box 一键唤醒导入与手机扫码导入，体验极致丝滑。

### 🌍 节点国旗 Emoji 智能识别与展示 (Auto GeoIP Flag)
- **国家地区国旗 Emoji 自动解析**：
  - 探针主控与代理中枢自动依据被控服务器的地理位置信息（`node.country`）或地区关键字（HK/JP/US/SG/TW/DE 等）动态解析并前置国家地区国旗 Emoji（🇭🇰/🇯🇵/🇺🇸/🇸🇬/🇨🇳/🇩🇪/🇬🇧 等）。
  - 全订阅协议（Clash YAML、Sing-box JSON、Base64）以及管理后台与用户面板的节点展示名均自动携带国旗图标，节点归属地一目了然。

### ⚙️ 节点创建弹窗极简降噪折叠 (Decluttered Modal)
- **默认视口极简清爽**：
  - 节点添加/编辑弹窗重构：默认仅展示「服务器、中转模式、协议类型、节点名称、监听端口」核心必填项。
  - 复杂的 SNI 伪装域名、Dest 目标、Reality x25519 密钥、UUID / 密码全量收纳进「⚙️ 高级配置 (点击展开)」折叠面板中，且默认自动完成安全密钥生成，实现开箱即用的极简操作。

---

## 🌟 [v2.3.0] - 2026-09-25

### 📱 移动端与全屏幕界面深度适配优化 (彻底消除横向滚动条)
- **管理员界面顶部防挤压重构**：
  - 修复此前手机窄屏下「管理后台」标签被竖向挤压换行的视觉缺陷。
  - 手机端隐藏该标签徽章并在大屏端以 `shrink-0 whitespace-nowrap` 保持单行胶囊徽章；「状态面板」自适应为紧凑图标按钮，节省出 60px 宽度。
- **用户端界面手机端自适应修复**：
  - 解决此前移动端用户面板由于顶部按钮过长导致向右溢出、用户必须向右拖动才能看见「退出」按钮以及卡片左侧被截断的问题。
  - 手机端「用户中心」徽章自动收起，「服务器探针」与「退出」在手机端自动精简为紧凑图标按钮，桌面端展示完整文本。
  - 全局注入 `overflow-x-hidden w-full`，彻底消除由于宽度溢出导致的横向滚动条，各屏幕尺寸下均居中对齐、无任何左右晃动。

### 🎨 双主题架构：经典默认恢复 + 浪漫樱花内置 + 跟随账号持久化
- **默认经典主题 (Default Slate)**：
  - 将 `:root` 和 `.dark` 变量完全回滚并还原为 ProxyProbe 原汁原味的**极简沉稳灰蓝经典风格**，作为系统全局的初始默认主题。
- **浪漫樱花主题 (Spring Sakura Blossom 🌸)**：
  - 樱花主题全部独立封装在 `.theme-sakura` 样式类中（浅色粉白温润唯美、深色夜樱深紫沉醉、专属粉嫩细滚动条）。
- **统一的【主题与外观风格】切换面板 (`ThemeSettingsDialog`)**：
  - 在管理员界面与用户界面顶部，新增了主题切换按钮（樱花主题下显示 🌸，默认主题下显示 🎨 调色板）。
  - 点击即可唤出精美弹窗，可自由切换「主题皮肤（默认经典 / 浪漫樱花）」与「颜色模式（浅色 / 深色）」。
- **设置跟随账号走 (云端数据库持久化同步)**：
  - SQLite 数据库 `proxy_user` 表新增 `theme_style` 字段，后端提供 `/api/proxy/theme` 接口。
  - 用户或管理员在任意设备（手机、平板、电脑）切换主题，选择均自动持久化保存在云端数据库中；再次登录时无缝还原个人喜好。

### 🔒 商业许可证离线验证与安全加固
- **Ed25519 顶级非对称离线鉴权**：
  - 探针基础监控功能永久免费，商业版高级特性（多服务器、多协议节点与客户端用户）采用 Ed25519 非对称离线签名验证。
  - 机器码指纹硬件特征绑定与单调时钟水印防回滚，确保授权安全与防破解。
- **管理面板商业授权极致精简**：
  - 管理后台「商业授权」界面专注呈现当前授权状态、资源配额进度条、机器识别码一键复制以及激活码核销激活。
  - 用户端（UserDashboard）完全物理隔离，绝不暴露任何授权配置，保障系统纯净与安全。

### 📋 剪贴板跨协议通用复制引擎 (彻底解决纯 IP / HTTP 复制失败问题)
- **根源修复**：现代浏览器出于安全策略限制，在纯 IP 或 HTTP 非安全上下文 (`http://IP:PORT`) 下将 `navigator.clipboard` 判定为 `undefined`，导致原有复制调用直接抛出异常崩溃。
- **通用降级引擎 (`src/lib/clipboard.ts`)**：
  - 优先侦测安全上下文原生 Clipboard API；
  - 非安全上下文无缝降级为无感 `<textarea>` 与 `document.execCommand('copy')` 机制，在移动端与桌面端均能 100% 顺畅复制。
  - 全量覆盖：管理员后台订阅复制、用户自服务面板一键复制、节点单链复制、机器码复制。

### 🎛️ 流媒体与 AI 解锁检测体系深度定制化 (完全可选与自选)
- **一键总控开关与表格自适应**：
  - 节点列表新增「流媒体/AI 解锁」配置入口与状态感知（实时展示已选 X项或已隐藏）。
  - 支持一键关闭检测列：关闭后在节点列表中彻底隐藏「流媒体 / AI」整列，横向空间全量释放给节点参数与操作按钮，呈现极致纯净表格。
- **自定义展示项与快捷方案**：
  - 修复原本即使清空也会强制回退为 4 项的硬编码缺陷，用户选择几项就精准呈现几项。
  - 新增一键快捷预设：「仅看两大 AI (Gemini + ChatGPT)」、「仅看主流流媒体 (Netflix + YouTube)」、「全选预设」、「清空已选」。
  - 前后端同步持久化保存用户定制偏好。

---

## 🌟 [v2.2.0] - 2026-09-24

### 🚀 节点管理与用户订阅体验大幅优化
- **订阅实时流量与到期提示节点 (置顶显示)**：
  - 用户在任意客户端（Clash / Meta / Sing-box / Shadowrocket / v2rayN 等）刷新订阅时，置顶直观展示实时状态。
  - 动态格式化剩余流量（`📊 剩余流量 | XX.XX GB (总计 XX.XX GB)` 或 `📊 剩余流量 | 不限量 (已用 XX.XX MB)`）。
  - 动态显示订阅到期时间（`⏳ 订阅到期 | YYYY-MM-DD` 或 `⏳ 订阅到期 | 永久有效`）。
  - **Clash 智能测速保护**：流量提示节点仅注入 `节点选择` (Select) 分组，严格排除在 `自动选择` (url-test) 测速组之外，彻底规避伪节点测速超时或红字报错。
- **节点命名纯净无前缀与可选前缀机制**：
  - 新建节点默认取消强制 `[Akile]` 前缀，保留纯净节点名（如 `Reality`、`Hysteria2`、`落地-Reality`）。
  - 新增「附加服务器名称前缀 (如 [Akile])」复选框，默认不勾选；用户可自由按需勾选开启。
  - 服务端订阅链接生成同步优化，直接使用自定义节点名称，杜绝多余硬编码前缀。
- **新建节点弹窗全响应式自适应重构 (彻底消除横向滚动条)**：
  - 弹窗外框采用 `w-[96vw] max-w-2xl max-h-[88vh] overflow-y-auto overflow-x-hidden`。
  - 落地目标选择、外部目标输入、协议选择等全部改造为 `grid-cols-1 sm:grid-cols-2` 响应式网格。
  - 对超长 IPv6 与落地连接加入 `truncate min-w-0` 截断保护，无论在移动端还是窄屏桌面端均自适应折行，无横向滚动条。
- **数据库备份文件导入与一键还原入口**：
  - 在管理后台数据备份模块增加「导入备份并还原」入口，支持直接上传 `.db` 或 `.db.gz` 备份文件。
  - 还原前自动安全核验 SQLite 结构及关键数据表，防止误传非法文件。
  - 还原前自动在服务器创建 `.pre-restore.bak` 安全快照，并热刷新数据，支持一键无损回滚。
- **双仓库架构与二进制防逆向编译保护**：
  - **私有仓库 (`ProxyProbe`)**：全量维护开发源码、交接文档与前端工程。
  - **公开发行仓库 (`ProxyProbe-Release`)**：严禁提交明文代码，核心代理模块全面通过 PyInstaller 独立编译为 ELF 机器码二进制 (`proxy-manager-linux-amd64`)，前端打包为静态归档包 (`web-admin-dist.tar.gz`)，从根源杜绝逆向破解。

---

## 🌟 [v2.1.0] - 2026-09-24

### 📦 核心功能：Telegram 机器人数据库定时自动备份与一键本地导出
- **Telegram 机器人数据库定时自动备份**：
  - 深度集成后台「通知渠道」配置，零额外配置直接复用已有的 Telegram Bot Token 与 Chat ID。
  - 支持多档备份周期：每 6 小时、每 12 小时、每天一次（默认推荐）、每 3 天一次、每周一次。
  - 支持单独指定专属接收 Chat ID（支持群组/频道或个人私聊，留空默认使用通知 Chat ID）。
  - 后台守护进程常驻调度，采用 SQLite Online Backup API 保证 WAL 事务一致性，Gzip 高倍率压缩生成 `.db.gz` 镜像包。
  - 自动推送包含站点名称、时间、文件大小、资产概况（服务器数、代理节点数、用户数）及还原指引的图文消息。
- **一键即时推送与本地副本导出**：
  - 提供「立即备份并推送到 TG」按键，一键打通测试与手动即时归档。
  - 提供「导出下载本地副本」按键，支持管理员在浏览器端直接导出下载数据库压缩包。
- **Caddy 路由与解压自愈机制**：
  - 规范 `/admin` 重定向至 `/admin/`，增强 SPA 路由健壮性。
  - 部署脚本和升级脚本新增前端解压自愈逻辑，自动展平目录层级。

---

## 🐛 [v2.0.1] - 2026-09-24

### Bug 修复
- **修复用户订阅链接 403 问题**：`proxy_manager.py` 中 `sub_url` / `clash_url` 域名写死，导致通过其他域名访问面板的用户订阅请求被路由到另一台服务器，因 token 不匹配返回 `HTTP 403 Invalid subscription`。
  - 新增 `get_request_base_url(headers)` 函数，动态读取 `X-Forwarded-Host` / `Host` 请求头，保证订阅链接始终指向用户实际访问的服务器。
  - 前端 `UserDashboard.tsx` 与 `ProxyUsers.tsx` 同步修复，复制订阅链接时统一使用 `window.location.origin` 替换域名，防止前端缓存旧域名。

---

## 📌 [v2.0.0] - 2026-09-24

### 💎 商业配额与安全体系重构 (Major Milestone)
- **免费体验版 (Free Tier)**：
  - 新增默认开箱免激活配额：**1 台服务器 + 1 个代理节点 + 1 个客户端用户**。
  - 探针监控功能全量永久免费，基础代理协议自由体验。
- **5 大商业套餐分级配额体系**：
  - **星尘套餐 (`stardust`)**：3 台服务器 · 6 个代理节点 · 6 个客户端用户。
  - **行星套餐 (`planet`)**：5 台服务器 · 12 个代理节点 · 12 个客户端用户。
  - **恒星套餐 (`star`)**：8 台服务器 · 18 个代理节点 · 18 个客户端用户。
  - **星系套餐 (`galaxy`)**：12 台服务器 · 30 个代理节点 · 30 个客户端用户。
  - **宇宙套餐 (`universe`)**：20 台服务器 · 50 个代理节点 · 50 个客户端用户。
- **吊销状态秒级即时同步与 0 配额熔断机制**：
  - 客户端后台心跳同步间隔由 12 小时缩短至 **30 秒**。
  - 客户端状态查询接口增加主动嗅探机制（距上次同步 > 20 秒立即在线核验）。
  - **吊销状态 0 配额硬熔断**：一旦被吊销，配额硬锁死为 `0/0/0`，杜绝穿透退回免费版。
  - **前后端双重 403 强拦截**：后端 `POST /api/proxy/nodes` 和 `POST /api/proxy/users` 拦截被吊销状态，前端弹窗与按钮红字警示并禁止提交。

---

## 📌 [v1.3.0] - 2026-09-23

### 🛡️ 商业授权体系与二进制闭源发布保护
- **Ed25519 非对称离线验签系统 (`license_guard.py`)**：
  - 基于 RFC 8032 Ed25519 纯数学算法，支持零依赖离线验签。
  - 集成硬件设备指纹识别算法（主板 UUID / DMI / 网卡 MAC）。
  - 内置单调递增时钟水印，杜绝客户通过回滚服务器系统时间逃避过期检查。
- **二进制闭源发布保护**：
  - 引入 PyInstaller 自动化编译流程，将 Python 核心逻辑打包编译为独立 Linux ELF 二进制可执行文件 (`proxy-manager-linux-amd64`)。
  - 彻底移除客户服务器上的明文源码，杜绝商业逻辑泄露。
- **双轨仓库部署分离**：
  - 私有仓库 `ProxyProbe` 保护核心源码。
  - 公开发行仓库 `ProxyProbe-Release` 提供编译好的可执行程序与前端静态文件包，实现无凭证一键部署。

---

## 📌 [v1.2.0] - 2026-09-23

### ⚙️ 运维工具链增强与网络协议优化
- **交互式控制台工具 `hhub`**：
  - 新增终端运维命令行 `hhub`（内置状态监控、服务启停、重置密码、修改端口、自动备份与一键升级）。
- **IPv4 / IPv6 双栈自由切换**：
  - 支持节点连接地址智能解析，提供 IPv4 优先、IPv6 优先、自定义域名解析与回退机制。
- **实时连通性与握手延迟测速**：
  - 节点列表新增【测试】功能，毫秒级探测全链路 TCP/TLS 握手及 UDP 响应。
- **端口自定义与自动化安全边界**：
  - 支持一键部署时自定义外部 Web 访问端口及内部端口，集成 Let's Encrypt 证书自动化校验。

---

## 📌 [v1.1.0] - 2026-09-22

### 👥 多用户管理与智能订阅分发
- **多租户权限与生命周期管理**：
  - 支持创建独立客户端用户，限制流量上限（GB）、到期时间与最大同时在线设备数。
  - 实时统计各用户上下行流量明细，超额/到期自动熔断断开。
- **全平台聚合订阅生成**：
  - 自动将所有启用节点聚合为标准订阅链接及 Clash YAML 配置文件。
  - 深度兼容 Clash Verge Rev、v2rayN、Shadowrocket、Sing-box、Loon、Surge 等主流客户端。

---

## 📌 [v1.0.0] - 2026-09-21

### 🚀 初始版本发布
- **探针监控与代理管理双系统融合**：
  - Rust 编写的高性能探针核心，极低内存与 CPU 开销。
  - Xray-Core 与 Sing-box 双核心调度，支持 VLESS Reality、VMess-WS、Trojan、Shadowsocks 2022、Hysteria 2、TUIC v5 等全套主流协议。
  - 现代化 Vite + React + Tailwind CSS 响应式管理面板。
